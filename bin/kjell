#!/bin/bash


SCRIPT_DIR="$( cd -P "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
CUSTOM_OTP_ROOT="${SCRIPT_DIR}/../deps/otp-custom_ttsl_drv"
 
$CUSTOM_OTP_ROOT/bin/erl -noinput -pa ${SCRIPT_DIR}/../ebin/ -run start_kjell 
